#write a python script to find number of digits in each of the elements in a given tuple of numbers
# Use map function


def length_of_digits(x):
    return int(len(x))
#print(tuple(map(length_of_digits,(e for e in input("Enter numbers seprated by comma:").split(",")))))
