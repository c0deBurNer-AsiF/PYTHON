#write a python script to compare two tuples, whether they contain the same elements in same order or not

t1=eval(input("Enter first tuple:"))
t2=eval(input("Enter second tuple:"))
print("Same order") if t1==t2 else print("not same order")
